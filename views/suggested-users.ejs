<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Suggested Users</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            padding: 20px;
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background-image: url('/images/user.png'); /* Replace 'background.jpg' with your image file name */
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
        }

        h1 {
            position: absolute;
            left:451px;
            top : 190px;
            font-size: 62px;
            font-family:'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
        }
        .brown {
            color: #4C2B08;
        }
        .white {
            color: #D7BDA6;
        }

        .card-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 50px;
            position: relative;
            top:125px;
        }


        .card {
            background-color: #4C2B08;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            width: 200px;
            padding: 15px;
            text-align: center;
    
        }
        .card img {
            width: 100%;
            height: 150px;
            object-fit: cover;
        }

        .card h3 {
            margin: 0;
            font-size: 18px;
            color: #D7BDA6;
        }

        .card p {
            font-size: 14px;
            color: #D7BDA6;
        }

        a {
            display: block;
            margin-top: 20px;
            text-decoration: none;
            color: #4C2B08;
            border: solid #4C2B08;
            padding:10px;
            position: absolute; 
            top: 86%; 
            left: 50%;
            transform: translateX(-50%)
        }
    </style>
</head>
<body>
    <h1>
        <span class="white">Su</span><span class="brown">gges</span><span class="brown">ted us</span><span class="white">ers</span>
    </h1>
    <div class="card-container">
        <% if (suggestedUsers.length > 0) { %>
            <% suggestedUsers.forEach(user => { %>
                <div class="card">
                    <img src="<%= user.imageUrl %>" alt="<%= user.username %>">
                    <h3><%= user.username %></h3>
                    <p>Favorite Cuisine: <%= user.cuisine %></p>
                </div>
            <% }) %>
        <% } else { %>
            <p>No suggestions available.</p>
        <% } %>
    </div>
    <a href="/">Logout</a>
</body>
</html>
